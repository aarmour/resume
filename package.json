{
  "name": "resume",
  "private": true,
  "description": "My resume",
  "author": "Adam Armour",
  "private": true,
  "scripts": {
    "export-html": "resume export -f html -t slick dist/aarmour.html",
    "export-pdf": "resume export -f pdf -t slick dist/aarmour.pdf",
    "export-all": "npm run export-html && npm run export-pdf"
  },
  "devDependencies": {
    "resume-cli": "^0.4.0"
  }
}
